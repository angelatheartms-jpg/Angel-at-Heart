export default function HomePage() {
  return (
    <main style={container}>
      {/* HERO */}
      <section style={hero}>
        <h1 style={title}>Angel at Heart Charity</h1>
        <p style={subtitle}>
          Spreading love, hope, and compassion to individuals and families in need.
        </p>

        <div style={buttons}>
          <a
            href="https://www.paypal.com/donate/?business=YOUR_PAYPAL_EMAIL&currency_code=USD"
            target="_blank"
            rel="noopener noreferrer"
          >
            <button style={primaryBtn}>One‑Time Donation</button>
          </a>

          <a
            href="https://www.paypal.com/donate/?business=YOUR_PAYPAL_EMAIL&currency_code=USD"
            target="_blank"
            rel="noopener noreferrer"
          >
            <button style={outlineBtn}>Monthly Donation</button>
          </a>
        </div>
      </section>

      {/* INFO CARDS */}
      <section style={grid}>
        <Card
          title="Our Mission"
          text="To serve with kindness and uplift hearts through community outreach."
        />
        <Card
          title="What We Do"
          text="Food drives, care packages, emergency assistance, and support."
        />
        <Card
          title="Get Involved"
          text="Volunteer, partner, or support us to make a difference."
        />
      </section>

      <footer style={footer}>
        © {new Date().getFullYear()} Angel at Heart Charity
      </footer>
    </main>
  );
}

function Card({ title, text }: { title: string; text: string }) {
  return (
    <div style={card}>
      <h3>{title}</h3>
      <p>{text}</p>
    </div>
  );
}

/* ---- styles ---- */

const container = { padding: "40px 16px" };

const hero = {
  textAlign: "center" as const,
  marginBottom: 64
};

const title = {
  fontSize: "2.8rem",
  color: "#db2777"
};

const subtitle = {
  maxWidth: 600,
  margin: "16px auto",
  color: "#555"
};

const buttons = {
  display: "flex",
  gap: 16,
  justifyContent: "center",
  marginTop: 24
};

const primaryBtn = {
  background: "#db2777",
  color: "white",
  border: "none",
  padding: "12px 26px",
  borderRadius: 999,
  cursor: "pointer"
};

const outlineBtn = {
  background: "transparent",
  color: "#db2777",
  border: "2px solid #db2777",
  padding: "12px 26px",
  borderRadius: 999,
  cursor: "pointer"
};

const grid = {
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))",
  gap: 24,
  maxWidth: 960,
  margin: "0 auto 64px"
};

const card = {
  padding: 24,
  borderRadius: 16,
  boxShadow: "0 6px 18px rgba(0,0,0,0.08)"
};

const footer = {
  textAlign: "center" as const,
  color: "#777",
  marginTop: 64
};
