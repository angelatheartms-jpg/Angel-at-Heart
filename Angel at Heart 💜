import { Heart, HelpingHand, Users } from "lucide-react";

export default function App() {
  return (
    <div style={{ fontFamily: "sans-serif", minHeight: "100vh" }}>
      {/* Hero */}
      <section style={{ padding: "80px 16px", textAlign: "center" }}>
        <h1 style={{ color: "#db2777", fontSize: "2.5rem", marginBottom: 16 }}>
          Angel at Heart Charity
        </h1>

        <p style={{ maxWidth: 600, margin: "0 auto 24px", color: "#555" }}>
          Spreading love, hope, and compassion to individuals and families in need.
        </p>

        {/* PayPal Buttons */}
        <div style={{ display: "flex", gap: 16, justifyContent: "center" }}>
          <a
            href="https://www.paypal.com/donate/?business=YOUR_PAYPAL_EMAIL&currency_code=USD"
            target="_blank"
            rel="noreferrer"
          >
            <button style={primaryButton}>One‑Time Donation</button>
          </a>

          <a
            href="https://www.paypal.com/donate/?business=YOUR_PAYPAL_EMAIL&currency_code=USD"
            target="_blank"
            rel="noreferrer"
          >
            <button style={outlineButton}>Monthly Donation</button>
          </a>
        </div>
      </section>

      {/* Mission */}
      <section style={{ padding: "40px 16px", maxWidth: 960, margin: "auto" }}>
        <div style={grid}>
          <Card
            icon={<Heart size={32} color="#db2777" />}
            title="Our Mission"
            text="To serve with kindness and uplift hearts through charitable outreach."
          />

          <Card
            icon={<HelpingHand size={32} color="#db2777" />}
            title="What We Do"
            text="Food drives, community support, care packages, and emergency assistance."
          />

          <Card
            icon={<Users size={32} color="#db2777" />}
            title="Get Involved"
            text="Volunteer, partner, or support us to make a difference."
          />
        </div>
      </section>

      {/* Footer */}
      <footer style={{ padding: 24, textAlign: "center", color: "#777" }}>
        © {new Date().getFullYear()} Angel at Heart Charity
      </footer>
    </div>
  );
}

function Card({ icon, title, text }) {
  return (
    <div style={card}>
      {icon}
      <h3 style={{ marginTop: 12 }}>{title}</h3>
      <p style={{ color: "#666" }}>{text}</p>
    </div>
  );
}

const grid = {
  display: "grid",
  gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))",
  gap: 24
};

const card = {
  padding: 24,
  borderRadius: 12,
  boxShadow: "0 4px 10px rgba(0,0,0,0.08)",
  textAlign: "center"
};

const primaryButton = {
  padding: "12px 24px",
  borderRadius: 24,
  border: "none",
  background: "#db2777",
  color: "#fff",
  cursor: "pointer"
};

const outlineButton = {
  padding: "12px 24px",
  borderRadius: 24,
  border: "2px solid #db2777",
  background: "transparent",
  color: "#db2777",
  cursor: "pointer"
};
